# Egy-atlagos-barat-boritek
Varázslatos Boríték (Magic Envelope)

Ez egy egyszerű, látványos és interaktív webes animáció, amely egy borítékot jelenít meg. Amikor rákattintasz, a boríték kinyílik, és egy kedves üzenet emelkedik ki belőle.

## 🚀 Telepítés és beállítás

A projekt futtatásához kövesse az alábbi lépéseket:

### 1. Előfeltételek
Győződjön meg róla, hogy a [Node.js](https://nodejs.org/) telepítve van a számítógépén.

### 2. Projekt letöltése
Töltse le a projekt fájljait egy mappába.

### 3. Függőségek telepítése
Nyisson egy terminált a projekt mappájában, és futtassa az alábbi parancsot:

```bash
npm install
```

Ez telepíti a `serve` csomagot, amely a fájlok kiszolgálásához szükséges.

### 4. A projekt futtatása
A szerver indításához futtassa:

```bash
npm start
```

Vagy ha manuálisan szeretné indítani:

```bash
npx serve -s . -p 5000
```

Ezután nyissa meg a böngészőjében a `http://localhost:5000` címet.

## 📂 Fájlszerkezet

- `index.html`: A projekt szerkezete és a boríték elemei.
- `style.css`: A dizájn és a kinyíló animáció logikája.
- `script.js`: Az interaktivitás (kattintás kezelése).
- `package.json`: A projekt leírása és a függőségek.

## ✨ Funkciók
- 3D-s boríték nyitási animáció.
- Reszponzív dizájn (mobilról is jól néz ki).
- Interaktív gomb és kattintható boríték.
- Letisztult, elegáns tipográfia.
